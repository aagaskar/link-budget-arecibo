/** Physical constants (SI). Values match the notebook in ../notebook/. */

/** Speed of light in vacuum, m/s */
export const C0_M_S = 299792458;

/** Boltzmann constant, J/K */
export const K_B_J_K = 1.380649e-23;

/** Planck constant, J/Hz */
export const H_J_HZ = 6.62607015e-34;

/** Meters per light year */
export const M_PER_LY = 9460730472580800;

/** Meters per astronomical unit */
export const M_PER_AU = 1.495978707e11;

/** Arcseconds per radian (3600 * 180 / pi) */
export const ARCSEC_PER_RAD = (3600 * 180) / Math.PI;

/** Seconds per Julian year */
export const S_PER_YEAR = 365.25 * 86400;

/** Nominal solar radius, m (notebook uses the same rounded value) */
export const R_SUN_M = 7.0e8;

/** Diameter of the Arecibo primary reflector, m */
export const ARECIBO_DIAMETER_M = 305;
